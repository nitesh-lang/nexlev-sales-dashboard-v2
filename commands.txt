http://127.0.0.1:8000

uvicorn main:app --host 0.0.0.0 --port 8000


git add main.py
git commit -m "Enable uploads temporarily"
git push origin main

git add main.py
git commit -m "Disable uploads (view only)"
git push origin main
